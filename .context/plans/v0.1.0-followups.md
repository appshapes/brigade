# Planned follow-ups to the 0.1.0 release

Compiled 2026-09-06 from a full sweep of the planning record. `.context/plans/brigade-execution-log.md` is the
authoritative source (archived whole into `brigade-execution-log-archive.md` on this date; line references below
resolve against the archived copy); the phase plan, proof results, release record (E5-release/E5-soak), changelog,
threat model and archive were read as supplements and every candidate was cross-checked against the log. Items the
log records as already done or resolved were dropped.

**Actionable now:** the two `todo` Status rows (P5-16, P5-19), the item-17 re-run whose trigger has already fired,
and the one-line staleness at `docs/security.md:496`.

## Named plan rows, status todo

- **P5-16 — Distribution channels.** A Homebrew tap (goreleaser `homebrew_casks`, possible now the repository is
  public) and a Linux equivalent (`.deb`/`.rpm` via `nfpms`, or the same tap through Linuxbrew — decide in the
  brief, still to write). The plugin bootstrap stays the primary install path and must not be shadowed by a tap
  install (E0-8 (e) measured the shadow); four re-opening conditions are recorded in
  `docs/experiments/E5-release.md` §12. (log:96)
- **P5-19 — Nine setup-document corrections from the distribution proof.** Nine sentences in `docs/setup.md`,
  `plugin/README.md` and the bootstrap's own output that do not match what a new user sees: foreground vs
  background first-use download; the registration line arriving on a later prompt on a cold cache; the
  undocumented `9 userConfig options not yet set` line; `team create --name/--label` documented only for
  developers; the `whoami` example's adapter name; the administrator sent to an in-session command before any
  session exists; `claude plugin marketplace remove` undocumented and the orphaned plugin copy; the publishable
  key's value in no document (by design — say so). Docs only; no patch release needed; plain language, no retired
  words. (log:97, items enumerated at log:1150–1158)

## Measurements deferred or waived at release

- **E0-10 hosted checks** — waived for 0.1.0 by Rjae (2026-09-05); the daily keep-alive and P5-1's hosted TLS
  conformance are the stated mitigation; the row stays unblocked as a candidate for after. (log:66, 2806–2807, 2844)
- **Item 17's third interactive run** — ruled "re-run once F1 is fixed"; the trigger has been met (P5-13 fixed F1
  on 2026-09-05) but no re-run is recorded. (log:780)
- **P5-12 interactive sweep** of the 26 test messages under the open frame level (~4.5 h, ~100 pty sessions) —
  owner-gated; runs only if Rjae calls for it. (log:1094, 2844)
- **Soak's lowered-`jwt_expiry` arm** (300 s) — deferred for lack of a stack restart that day; E0-6's measurement
  stands as the only evidence. (log:1208)
- **E2E-09's auto-mode arm** — the outbound ask-rule measurement in auto permission mode was never run; named as a
  gap in `docs/security.md` §11; no row schedules it. (log:94, 967)
- **Two-machine hosted smoke run** — an owner step still open in the phase plan.
- **Six distribution-proof arms** stayed unmeasured after the tag (E5-release), and **E2E-06's model half is at
  n = 1**.

## Open findings — documented, no fix scheduled

- **Item 21 (acknowledgement-bait bare receipt)** fails at every frame level (six receipts in eleven runs across
  open/guarded/strict). Ruled 2026-09-06: the default stays `open`, no code change; open, non-blocking finding in
  `docs/security.md` §4 and §11. Note: `docs/security.md:496` still reads "For Rjae's decision" — recording the
  ruling there is a small pending docs fix. (log:109, 1144–1146)
- **Claude Code's 50-post mid-turn inbox queue loss** — 2.1.261 silently drops inbox posts beyond 50 queued while
  a turn is in flight, after Brigade has acked them (9 of 60 frames, reproduced three times). Documented known
  limit of 0.1.0; the product question (pace or hold posts while the session is busy — the watcher knows the
  activity flip — versus restating the bound) stays open. (log:2745, finding at 1210–1218)
- **Watch the `make test` flake** in `internal/harness/watch` (E5-release).
- **Process tree of a first session** still owed by the after-tag proof (E5-release).

## Engineering carry-overs from the Phase 4 findings

- **F4 — prune SIGKILL residue** (by-pid map, seen file, socket, pidfile). Assigned "(Phase 5 prune)"; no Status
  row or DONE section records it landing — P5-14 explicitly left old per-pid seen files unpruned and P5-9 notes
  `brigade inbox` lists crash-survived maps as "F4's prune territory". (log:767)
- **F5 — make `resumed:true` observable** — the SessionStart hook discards it; the proposed fix is one log line.
  (log:768)
- **F8 — sandboxed access to the local stack** — dropped for v1 (`client.go:96` still honours `NO_PROXY`; the
  hosted-domain entry is the documented sandbox story). Revisit if a developer needs the local stack from a
  sandboxed session. Related deferred measurement: the sandbox `allowedDomains` entry against a hosted project.
  (log:788)

## Standing / conditional obligations (trigger-driven)

- **Exercise the published-release arm of `checksums-check` rule (c)** — never run in CI (the release-record
  commit passed by the fresh-build arm); it engages on the first post-release commit that changes Go source
  (P5-19, docs only, will not). Confirm it behaves then.
- **Keep-alive monitoring** — GitHub disables scheduled workflows after 60 days without repository activity on the
  now-public repository.
- **Go toolchain bump ⇒ version bump** (the go-line release-reproducibility pin), and the **shellcheck 0.9.0
  re-pin rule**: re-pin CLAUDE.md's sentence and `scripts/ci/README.md`'s copy whenever ci.yml's "Runner image
  inventory" step prints a different version. (CLAUDE.md)
- **CI conditionals:** re-base `timeout-minutes` on Blacksmith hardware; trim the macos job if it exceeds five
  minutes; add an environment protection rule for deploy-staging secrets; add the same-repository condition on
  `pull_request` jobs; watch for "adapter did not finish within its deadline" on slow Linux runners; convert the
  two remaining timing bounds to hang catchers if they trip.
- **Re-verify the undocumented session-registry usage on each Claude Code release** (threat model).
- **Revisit Claude Code Channels** as a delivery path if it leaves research preview.
- **Raise the 16 KiB body cap** and add the **Before User Created hook / bootstrap-token fallback** for anonymous
  sign-up floods only if needed (threat model).
- **Owner/mode check on a symlink-followed `frame_file`** (candidate hardening).

## Docs, protocol and test backlog

- **BAP/1.x question backlog** ((a)–(n) plus suite gaps) for a future protocol-revision pass, answered as second
  implementations surface them; collect **adapter-contributor review** of the frozen core for expressibility gaps.
- **Conformance `[no case]` markers** left in the frozen protocol document: B-3, B-5's event half, B-11 — no row
  scheduled. (log:108; docs/protocol-v1.md:356, 540, 575, 679, 766)
- **Small doc/plan fixes:** four stale 09-testing.md citations in E4-crash-resume.md; name the `api-refused` class
  in 9.6's corpus pass rule; the clause at 08-phases.md:116 placing P4-3's numbers outside the ten criteria; the
  one-word version drift in the experiments README's Phase 4 heading; document `CLAUDE_PLUGIN_OPTION_*` planting
  and the two redactor limits in the threat model; align the `permissive_rls_policy` lint mirror with Supabase's
  0024/0006.
- **Test debt:** the coverage-floor decision for `internal/protocol` and `internal/harness/inbound`; no positive
  test for the fs adapter's `--prompt` TTY path; P1-8 bootstrap/CI residual gaps; the fs-adapter tests' 5 s spawn
  budgets (candidate row); the reason-token inconsistency in `Instruction.Validate`; re-evaluate the
  belt-and-braces `utf8.Valid` check in the adapters' stdin reader.

## Longer-horizon deferred features and housekeeping

- Session-scoped team/profile switch.
- A reuse checklist for an existing Supabase project.
- Threat-model hardening deferred by design: join-event visibility in the session list; inactivity/time-box
  session settings for stale principals; optional end-to-end body encryption with a team-shared key.
- The three P6 conventions declined on cost but re-openable: build badge, composite action, YAML rewrite.
- **Private vulnerability reporting toggle** for the repository.
- Delete the **two empty throwaway teams** left on the hosted project by the distribution proof.

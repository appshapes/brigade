# Planned follow-ups to the 0.1.0 release — critical and high only

Reduced 2026-09-06 to the critical/high items at Rjae's request. The full sweep (every deferred measurement,
conditional obligation, docs/test polish item, threat-model deferral and housekeeping note, with log citations)
is preserved at commit `4b11391` of this file. `.context/plans/brigade-execution-log.md` remains the authoritative
Status record; the archive holds everything through the release.

**Not listed here:** the admin/member setup-UX discussion opened after the release. The per-session team join was
walked back on 2026-09-06 (profile:team 1:1 stands; per-project selection satisfies the requirement); the live
candidate is a Brigade-owned project→profile map read by the SessionStart hook, because Claude Code ignores
`pluginConfigs` in project and local settings since v2.1.207 (docs/research/claude-code-docs-gaps.md:85-87) and
`/plugin configure` is user-global. In-session `team join`/`profile init` with `--secret-file` remains open as a
convenience question. That work gets its own brief, and the docs-accuracy workflow that follows it absorbs P5-19.

## Actionable now

- **P5-19 — setup-document corrections** (Status row, todo). The nine sentences that do not match what a new user
  sees, plus a tenth found after: `docs/security.md:496` still says "For Rjae's decision" though the frame-level
  ruling landed 2026-09-06 (default stays `open`). To be absorbed into the post-UX docs-accuracy workflow rather
  than run standalone.
- **Item 17's third interactive run** — ruled "re-run once F1 is fixed"; P5-13 fixed F1 on 2026-09-05, so the
  trigger has fired, and no re-run is recorded. (archive: P4-6)

## Committed work, not yet landed

- **P5-16 — distribution channels** (Status row, todo). Homebrew tap + Linux packages; brief to write; the plugin
  bootstrap stays the primary path and must not be shadowed (E0-8 (e)); four re-opening conditions in
  `docs/experiments/E5-release.md` §12.
- **F4 — prune SIGKILL residue** (by-pid map, seen file, socket, pidfile). Carried into Phase 5 as "(Phase 5
  prune)" and never landed; P5-14 explicitly left old per-pid seen files unpruned.

## Obligations to watch

- **`checksums-check` rule (c), published-release arm** — never exercised in CI; it engages on the first
  post-release commit that changes Go source. Confirm it behaves when that commit lands (the UX work above will be
  that commit).
- **Keep-alive under GitHub's 60-day rule** — scheduled workflows are disabled after 60 days without repository
  activity; a disabled keep-alive lets the free-tier hosted project pause.
- **Claude Code's 50-post mid-turn inbox queue loss** — acked frames silently dropped beyond 50 queued during a
  busy turn; documented known limit of 0.1.0, and the product question (pace/hold posts on the activity flip vs
  restating the bound) is still open.
- **Private vulnerability reporting** — the repository toggle is still off; one click, and the repo is public and
  security-sensitive.

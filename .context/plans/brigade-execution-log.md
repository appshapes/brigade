# Brigade execution log

The single source of truth for **where the work stands**. The plan says what to build; this file says what is done,
what is next, and which model tier each remaining task should run on. Update it in the same commit as the work.
**Exception (Rjae, 2026-09-09): a pull request labelled `maintenance` — the agentic workflows' own PRs,
dependency bumps included — carries no row.**

Ticket 15 · branch `master` · merges only, never rebase · commit messages `15: <Imperative summary>` ·
`make push message="15: …"` for every commit after the first.

**0.5.0 is released (2026-09-10: the session model and context columns, the schema-compatibility rule, BAP/1 additive); 0.4.1 was released 2026-09-08 (after 0.3.0 and 0.4.0 the same day), 0.2.0 on 2026-09-07 and 0.1.0 on 2026-09-06.** The full record through the release — every Status row, DONE section, plan
correction and ruling — was moved verbatim to `.context/plans/brigade-execution-log-archive.md` on 2026-09-06;
a reference of the form `see "P5-10 DONE — 0.1.0 RELEASED"` resolves there; on 2026-09-08, after 0.4.1, the twenty-one
rows that carried the 0.2.0–0.4.1 work (P5-16, P5-19, the eighteen P7 rows P7-1..P7-16 with 6b, 6c and 11b, F4 — every one `done` or `won't do`) followed, verbatim,
below the archive's marker dated 2026-09-08; on 2026-09-09 the Codex workstream's two rows (P8-1 `won't do`, F5 `done`) followed,
below the marker dated 2026-09-09. The complete post-release work list
(deferred measurements, open findings, conditional obligations, backlog) is
`.context/plans/v0.1.0-followups.md`; this file carries only the rows being actively worked.

## Start here (for a fresh session, a resumed session, or another developer)

1. Read `.context/plans/claude-code-team-messaging-implementation-plan.md` — the index — then
   `implementation/02-decisions.md` (decisions, all marked **Decided 2026-08-30** are settled),
   `implementation/08-phases.md` (phases and tasks), `implementation/13-commit-plan.md` (the commit plan).
   `.context/plans/claude-code-team-messaging-logical-plan.md` is the conceptual design behind it.
2. Read `docs/research/README.md` for what the eleven digests contain and `docs/research/decisions-2026-08-30.md`
   for the decision brief (it overrides the plan where they disagree).
3. Read `.context/plans/v0.1.0-followups.md` for everything open after 0.1.0, then find the first row below with
   status `todo`, run it at the model tier in its column, and update its row.
   On 2026-09-08 Rjae closed P5-16, P7-6c and F4 after the follow-ups triage (P7-16) under the bar that an item must be
   proven high value to be worked, and the twenty-one finished rows moved to the archive. On 2026-09-09 Rjae closed the
   Codex workstream (P8-1, `won't do`; F5 `done`; both in the archive): Codex has no supported way to wake a live
   conversation, so a Codex member could only be a mailbox, never a peer. OpenAI models are used by keeping Claude Code as
   the session and adding OpenAI's `openai/codex-plugin-cc`; Brigade needs no code for that. The brief
   (`.context/plans/codex-team-participation.md`) and its P0 evidence (`docs/experiments/codex-participation.md`) stay as
   the record. On 2026-09-10 Rjae chartered the agentic-workflows brief (`.context/plans/agentic-workflows.md`, v4) and
   rows P9-1..P9-7 were opened and done the same day, in one commit. On 2026-09-10 Rjae also ruled that
   `brigade sessions` should report the **model** and the **context**, and — revising the same day's first ruling —
   that the **transcript is the source**: a plugin cannot supply `statusLine` (only `subagentStatusLine`), so the
   statusline route would have needed every member of every team to hand-edit their own `settings.json`, and a
   roster column that is blank for whoever did not is worse than no column. **Usage limits and the context window
   are dropped** — neither exists on disk at all. Reading the transcript needed no T10 exception: T10 governs what
   goes on the wire, and it was `hook.go`'s own "never read" comment, stricter than the rule it cited, that was
   relaxed. The brief is `.context/plans/session-properties.md` (**v2**, shipped); its rows **P10-1..P10-4 are
   `done`**, landed in one commit, and **P10-5** (schema compatibility in both directions, Rjae's ruling the same
   day) followed in the next; the administrator's plugin-only migration path is charted as
   `.context/plans/backend-upgrade.md` (**P12-1..P12-3, awaiting charter**). **P11-1** (the one-team-many-repositories feasibility study,
   `.context/plans/multi-repo-team.md`, Frank's session) is **`done`** — the answer is "works as-is, docs only",
   recorded in `docs/experiments/E6-multi-repo.md`; the docs change it names landed as **P11-4** and the repository name on the roster as **P11-5** (Trello card 18).
   After P10 the next substantial work is a
   second adapter, which Rjae will chart as its own brief; nothing is to be started on it until then.

## Model tier policy

The user's Fable 5 usage is a hard budget (64% consumed at the time of planning). Run **Fable 5** only where the
quality difference matters; run **Opus 5** wherever it can succeed. If the Fable budget runs out while
Fable-tier tasks remain, **stop and wait for the reset** rather than downgrading them — the user stated this
explicitly and prefers waiting to a subpar implementation. The user may also hand the work to another developer
with her own Fable 5 session on this machine, which is why this log lives in the repository.

- **Fable 5**: protocol/schema design, Supabase SQL (RLS, RPCs, triggers), the security path (frame, sanitiser,
  socket poster, inbound policy, watcher), the conformance suite's design, adversarial review/verification passes,
  and any task whose acceptance criteria include a negative security test.
- **Opus 5**: scaffolding, Makefile/CI/goreleaser, the bootstrap script, docs, running scripted experiments and
  recording their results, test plumbing, mechanical ports, evidence collection.

**Cadence within a Fable-tier task (decided by Rjae, 2026-08-30).** Run **one author agent plus one full adversarial
verifier** — roughly 4–6 Fable subagents per task. No multi-lens design panels, no separate completeness critic. The
evidence for putting the budget here: the adversarial pass found **7** assertions passing for the wrong reason in E0-1
and **15** in E0-2 (including a soak that would have passed having sent zero messages, and a "50 concurrent senders"
claim that was never actually measured). Both authors reported fully green beforehand and neither caught its own gaps.
The design panels, by contrast, mostly yielded nice-to-have extras. So when the budget is trimmed, the verifier is what
survives. Prompt the verifier to assume a pass is for the wrong reason, to check that each assertion *could* fail, to
demand positive controls, and to strengthen weak checks in place before reporting. When the driver session's main loop
is a smaller model, Fable-tier work goes to subagents (`model: 'fable'`), never inline.

## Status

Legend: `done` · `todo` · `blocked (<reason>)` · `wip` · `won't do (<date>)`. Every row through the 0.4.1 release and the Codex workstream is in the archive; the seven P9 rows below were opened and closed together on 2026-09-10, and the four P10 rows below them were opened and closed together the same day, in one commit (see Start here, step 3).

| ID | Task (plan §, named in the row) | Status | Model | Commit / evidence |
| --- | --- | --- | --- | --- |
| P9-1 | GitHub configuration: verify the two repository secrets (both already set, 2026-09-10) and **note their renewal dates**, set the squash-merge title source (the only setting that actually changes), the three labels, the outside-collaborator UI gate, the `master` ruleset (agentic-workflows brief §4.10 (1)–(7)) | done | Opus | `gh secret list` and the `gh api` read-backs recorded in `docs/experiments/agentic-workflows-first-runs.md` (ruleset `master-pr-checks`, id 22773942; squash title `PR_TITLE`, body `PR_BODY`; the three labels); the outside-collaborator approval was already `all_external_contributors`, so no UI step was needed; renewal dates recorded there (PAT none, OAuth token 2027-09-10); §5 (c) answered from `thinktech-api` in the same file |
| P9-2 | **Everything in the brief's §4, in ONE commit** (§4.10 (8) — no staging, no burn-in): the **eight workflows** `claude.yml` (two jobs, `agent` → `open-pr`), `review-pull-request.yml` (including the `gh pr comment` on a guard trip), `auto-merge.yml`, `_create-claude-issue.yml`, `update-documentation.yml` (`auto_merge: true`), `review-repository.yml` (monthly cron live), `release-notes.yml` and `update-dependencies.yml`; plus `scripts/ci/pr-guard.sh`, the three `.claude/agents/*.md`, and **every record line** — `scripts/ci/README.md`'s eight workflow rows, its one script row, its two configuration rows and its opening-sentence counts, `CLAUDE.md`'s paragraph, and this log's exemption sentence; resolve the `claude-code-action` SHA pin (7.7) | done | Opus | this commit — the eight workflows, `scripts/ci/pr-guard.sh` (mode 100755), `.claude/agents/{reviewer,documenter,developer}.md` and every record line above; `make plugin-check lint test` green; `pr-guard.sh` through both shellchecks (local **and** the pinned 0.9.0 container) |
| P9-3 | The loop's provisional budgets (`timeout-minutes`, `--max-turns`) pinned in the workflow files; records (a)–(d), (i) and (j) — and the re-pin from ten real runs — follow on the first runs | done | Opus | this commit — the loop is live and the provisional budgets are pinned in it; first runs to be recorded in `docs/experiments/agentic-workflows-first-runs.md` as they happen. Adds no file, so no `scripts/ci/README.md` row Records (a), (b) and (c) landed 2026-09-10 from the first live run — (c) proven here: the merge waited 24 s past the reopened CI run under the PAT; (i) and (j) pending their first occurrence. |
| P9-4 | `update-dependencies.yml` and `.claude/agents/developer.md`'s dependency-update mode; records (e) and (f) — rule (c) green through the published-release arm, the plugin serving the last release until `make release` — follow on the first run | done | Opus | this commit — `update-dependencies.yml` and `.claude/agents/developer.md`'s dependency-update mode; first runs to be recorded in `docs/experiments/agentic-workflows-first-runs.md` as they happen. Adds no file |
| P9-5 | **Remedy (a) of the brief's §4.7**: BOTH edits to `release.yml` — the widened top-level `permissions:` and the `notes` job that calls `release-notes.yml` — plus record (g) | done | Opus | this commit — `release.yml`'s `permissions:` widened to `contents`/`issues`/`id-token` and the `notes` job added, passing exactly one named secret; first runs to be recorded in `docs/experiments/agentic-workflows-first-runs.md` as they happen. **First run 2026-09-10 (v0.5.0): the `notes` job FAILED** — `claude-code-action` refuses the tag-`push` event a `workflow_call` job inherits ("Unsupported event type: push", run 34537045037), and the `release: published` trigger cannot fire from a `github.token` publish; **fixed** in the next commit by dispatching `release-notes.yml` (`workflow_dispatch`, one of the two events the job token may raise) as `release.yml`'s last step under `actions: write`, the widened `issues`/`id-token` ceiling dropped; the hand dispatch for v0.5.0 (run 34537527402) wrote the notes and opened issue #4 — record (g). **Second pass on v0.5.1:** the dispatch fired from release.yml (run 34539842793) and the action refused the `github-actions` actor — `allowed_bots` gains `github-actions`, as the reviewer's did on the first run; the next release is the first unattended one. Pre-release arm still unmeasured |
| P9-6 | `update-documentation.yml` (`auto_merge: true`) and `.claude/agents/documenter.md`; record (h), both halves, follows on the first run | done | Opus | this commit — `update-documentation.yml` (`auto_merge: true`) and `.claude/agents/documenter.md`; first runs to be recorded in `docs/experiments/agentic-workflows-first-runs.md` as they happen **First run 2026-09-10: PASS** — dispatched by hand; issue #2 → PR #3 → `CHANGES_REQUESTED` → fix `b82edd7` → `APPROVED` → squash-merged as `4a9d5a3`, issue closed by the merge; record (h) in `docs/experiments/agentic-workflows-first-runs.md`, with the two first-run findings (the fork-approval policy set to `first_time_contributors`; the reviewer's `allowed_bots` gains `github-actions`, b3538a0). |
| P9-7 | **The seeded memory marker** `.context/plans/agent-memory/review-repository.md` (`Date: (unseeded)`, `Commit: (none)`, `Scope: whole tree`) and the first `review-repository` pass | done | Opus | this commit — the seeded marker and `review-repository.yml` (monthly cron live, no `auto-merge` label); first runs to be recorded in `docs/experiments/agentic-workflows-first-runs.md` as they happen |
| P9-8 | **Release notes get a gate** (Rjae, 2026-09-10, after the v0.5.0 notes named a `/brigade:sessions` skill that does not exist): the writer agent DRAFTS to a file outside the checkout; `scripts/ci/release-notes-lint.sh` refuses anything that does not exist (a skill, a CLI verb, an asset), a shipped asset left unnamed, a missing version, "Unreleased", a placeholder, anything secret-shaped; a reviewer agent answers APPROVED or CHANGES_REQUESTED against `CHANGELOG.md`, the tag's log and the plugin tree; exactly one fix cycle; the publish and the announcement issue are shell steps (idempotent: a re-dispatch rewrites the notes and opens no second issue); both agents at `--effort xhigh`; the `release: published` and `workflow_call` triggers dropped (both dead, measured) | done | Fable | this commit — `release-notes.yml` (14 steps, `workflow_dispatch` only), `scripts/ci/release-notes-lint.sh` with `release_notes_lint_test.go` (twelve refusals, each with the good notes as its control, and the plugin tree as the skills authority when no list is given), both shellchecks green. **Exercised on v0.5.1:** run 34541118273 failed at the verdict — the action honours `Bash(<prefix>:*)` rules and not `Write(//tmp/…)` rules, two denials per agent, no file, **nothing published** (the gate held); `64f5b2a` moved the writes to `tee` under a Bash rule and kept each agent's execution log in the artifact; run 34541572413 then ran the whole path — the reviewer refused round 1 ("one fix" against the changelog's two), the fixer corrected it, round 2 approved, published, no second announcement. **On v0.5.2** the first unattended dispatch (run 34583310273) reached the reviewer, which found a real inaccuracy in 28 turns and was then failed by the action's `--max-turns 20` after writing its verdict; `d684bca` gives the agents 50 turns and lets the files decide (`continue-on-error`; a missing review is `CHANGES_REQUESTED`); the re-dispatch (run 34583822028) was approved in round one and published (issue #6). Recorded in `docs/experiments/agentic-workflows-first-runs.md` |
| P10-1 | **The transcript reader and its plumbing** (`.context/plans/session-properties.md` §5.1, §5.2): the new `internal/harness/transcript` package (incremental NDJSON reader — regular-file check so nothing can block on a FIFO, byte offset, complete lines only, a 64 MiB line cap, truncation reset, diagnostic-only errors that never carry the path), `transcript_path` on the hook's stdin type and in the 0600 by-pid map (`Validate` refuses a relative path; `whoami` does not expose it), and the watcher holding one reader on the command-writer goroutine, refreshed immediately before each heartbeat on **both** heartbeat paths | done | Fable | this commit — the reader with its unit tests in `t.TempDir()` (full id kept, a `/model` switch, a bare id that disagrees with the attachment, no attachment, a sidechain ignored, a partial trailing line, truncation, a non-JSON and a > 64 MiB line, a FIFO refused, a missing file); hook/`sessionmap`/watcher plumbing and the watch-rig tests through the fake adapter (a map with `transcript_path` → a heartbeat carrying both members; an appended switch attachment changes the model on a later heartbeat; a map without it sends neither). **Adversarial pass (security lens), fixed in place:** a negative or overflowing usage count made the watcher send a heartbeat the adapter refuses **whole**, lease renewal included — a corrupt usage is now skipped, the sum saturates, an out-of-range count is left absent; json/v2's strict default dropped records carrying a lone surrogate, an invalid byte or a duplicated member, all of which Claude Code legitimately writes — decoded leniently now; the heartbeat tests assert that every recorded heartbeat passes `WatchCommand.Validate` and that neither heartbeat path's raw stdin or argv carries the path. **Measured after the pass:** Claude Code writes `<synthetic>` assistant records with an all-zero usage for a turn the API refused (five across real transcripts) — the reader skips them, else one would have reported `&lt;synthetic>` and `context=0`. **The row's v1 text was a statusline cache: there is no such cache and no `brigade statusline` subcommand** — §4 of the brief records why the source changed |
| P10-2 | **The wire change, in ONE commit** (`docs/protocol-v1.md:3-9`): `model` and `context_used_tokens` — optional, nullable, `omitzero` — on `SessionRegistration`, `SessionRecord`, `HeartbeatRequest` and the watch `heartbeat` command plus their `Validate` arms; `MaxModelChars` = 128 and `MaxContextUsedTokens` = 2^53 − 1; `limits.max_model_chars` (and deliberately no limits member for the token bound); `protocol.SanitizeModel`; the schema patches and `make schema`; `docs/protocol-v1.md` with its byte-identical `internal/protocol/testdata/examples/*.json`; conformance **C-44**; **both** adapters including the new Supabase migration (`.context/plans/session-properties.md` §5.3–§5.6) | done | Fable | this commit — the four shapes, 12 new `TestValidationArms` subtests, `make schema` + `make schema-check` green, the five example documents and every `docs/protocol-v1.md` block byte-identical, C-44 (`internal/conformance/cases/c44_model_context.go`, two `cap:` tags, skipped when either is missing) taking the suite to **46 cases**, both adapters advertising `session.model` and `session.context_used_tokens`, and a **new** migration `_session_model_context.sql` that drops and re-creates `register_session` / `session_heartbeat` with the two parameters **appended with defaults** — so the positional pgTAP calls still work and `supabase/tests/functions.sql`'s 28-function, no-overload assertion still holds. Both v1 traps were real and both were handled: the full argument-type `grant` lists, and `schema.patchProp`'s loud failure on an unpatched member |
| P10-3 | Display (`.context/plans/session-properties.md` §5.5): `sanitizeRecord` in `internal/harness/commands/format.go`, the human line in `sessions.go` (`model=…` after `principal=…`, `context=…` before `seen …`, both only when present), and the goldens | done | Opus | this commit — `modelLine` / `tokensLine` (189681 → `190k`, 1500 → `2k`, 999 → `999`), `sessions.txtar` giving two of its four canned records the new members — alice `claude-opus-5[1m]` / 189681 and carol a **hostile** model string with `<system-reminder>` tags and a newline, which comes back neutralised — with `want-sessions.txt` and the JSON assertions updated byte for byte; plus `"max_model_chars":128` added to the 12 hand-written inline `limits` objects in eight `cmd/brigade` txtar files, which a new required `limits` member had made `describe_invalid` (found red by the wire-lens adversarial pass, which also made the spec's `session list` composite check bite — each listed record must carry the exact `SessionRecord` member set — and added a committed-schema bounds test over the four defs at 128/129 code points and 0 / 2^53 − 1 / −1 / 2^53). `modelLine` shows the model exactly as `nameLine` shows a name — tags neutralised, `>` kept, one line — after a first cut that also dropped the attribute breakers. **Two items in the v1 row were not needed:** `whoami` (neither member is on it) and the 20 `scripts/ci/testdata/proof-crash-resume/*/sessions/*.json` fixtures — two new **optional** members change no existing document, so records without them print exactly what they printed before |
| P10-4 | User-facing docs and the record (`.context/plans/session-properties.md` §6): `docs/adapter-authors.md` everywhere it mirrors the wire, `docs/security.md`, `CHANGELOG.md`, and the brief and this log | done | Opus | this commit — `docs/adapter-authors.md`: 45 → **46 cases** in the four sentences about the suite's size (the two measured transcripts keep their numbers and now say which run they are), both capability strings in the `describe` example and the `--json` report, `max_model_chars` in the limits object and table, two capability rows, two `SessionRecord` rows, two registration rows, the "two facts, not a channel" paragraph, the multi-`cap:`-tag rule C-44 introduces, and a harness-contract paragraph on what rides the heartbeat; `docs/security.md`'s "What is deliberately never sent" kept true and extended with what Brigade now reads **locally**; `CHANGELOG.md` gains an `## [Unreleased]` section including the administrator's migration step, named against `docs/setup.md`'s *Deploying the backend*. No statusline wiring is documented anywhere: there is none |
| P10-5 | **Schema compatibility in both directions** (Rjae, 2026-09-10: releases must be schema backward compatible; we do not control which adapter version and which schema version an installation pairs): a migration only ever **appends** RPC parameters with defaults; the adapter names an appended parameter only when it has a value (`p_model`, `p_context_used_tokens` omitted when nil, so every registration the harness sends matches the older signature unchanged); on `PGRST202` it sends the call again without them — the values are dropped, never the heartbeat, so no lease is lost to a missing migration — warns once on stderr naming the migration, remembers the backend as legacy for ten minutes in the profile directory, withholds the two capabilities from `describe` meanwhile, and clears the marker on the first success with the parameters | done | Fable | this commit — `internal/adapters/supabase/compat.go` with `compat_test.go` (a registration without facts names neither parameter; a legacy backend: one probe, one fallback, a valid result, the marker stamped with the adapter's clock, the warning once and not on the next call; a registration with facts falls back too; an expired or future-stamped marker probes again and a migrated backend clears it; a fresh marker skips the probe; `describe` withholds the two capabilities on a fresh marker and not on a stale one, creating and removing nothing); the three "named and null" pins in `session_test.go` and `watch_test.go` re-cut to "omitted when absent". **Deployed the same day:** migration `20260910193200_session_model_context` applied to both hosted projects through `make backend-install` (`wmgtaraqmoufmrnyojzf` and `hsopqzvznxajeyswzztv`; six migrations `local == remote` on each, every setting already correct, zero PATCHes, no token in the log). What remains is how an administrator applies a migration **from the plugin alone** — `.context/plans/backend-upgrade.md`, P12-1..P12-3 |
| P10-6 | **An update must reach a running session** — found by Rjae after `/brigade:update` on two sessions: no `model=`/`context=` columns. Measured: every running watcher on the machine was the 0.4.1 binary (or the dev build), one by-pid map of five carried `transcript_path` (its hook had already run under 0.5.0), and no session record anywhere carried `model` — the SessionStart hook respawned a live watcher only when its socket or token changed, and the prompt hook only when it was dead, so a plugin update reached a session's watcher when the session ended | done | Fable | this commit — `pidfile.Entry.Version` (`buildinfo.String()`, written by `watch/guard.go`; a pidfile without it reads as "" and is never current); `hook/start.go` `respawnReason` (coordinates changed, or version changed) and `hook/prompt.go` `ensureWatcher` replacing a live watcher of another version within the prompt budget; tests for both hooks (another version, no version, the same version kept) and the pidfile round trip; the rig's fake spawner writes the version like a real watcher. The v0.5.0 notes' non-existent `/brigade:sessions` corrected on the release page and guarded in `release-notes.yml`'s prompt; `plugin/skills/update/SKILL.md` says when the watcher follows. Released as **0.5.1** |
| P10-7 | **A replaced watcher must not leave its session closed** — measured 2026-09-11 by running 0.5.1's prompt hook by hand on this session (the only way to exercise it, since `/reload-plugins` is a user command): the hook logged `watcher version changed; respawning`, the 0.4.1 watcher exited **and closed the session** (its exit path owes the team a close), and the replacement heartbeated `conflict` every 30 s — the session dropped off the roster. The SessionStart path had papered over the same closure with a heartbeat-then-re-register the prompt hook has no budget for | done | Fable | this commit — `internal/harness/watch/reopen.go`: on a heartbeat (or any command) answered `conflict:session_closed` or `not_found`, one `session register` with `resume.session_id` the watcher's own id, the current name/activity/inbound/lease and the transcript facts; a resumed answer with the same id is "session re-opened" and the next heartbeat follows; a fresh id is closed best effort and the watcher stops with `session_gone`, as it does on `not_found`/`session_live`; both heartbeat paths (RPC and stdin error event) trigger it, `reopening` keeps them from racing. Tests: the RPC path (conflict → resumed register → heartbeats resume), the stdin path (an error event), and the gone case (not_found → stop). **Live:** this session was healed through the SessionStart path meanwhile and shows `model=claude-fable-5-1  context=802k`. Released as **0.5.2** |
| P11-1 | **Feasibility study: one Brigade team spanning several repositories** (`.context/plans/multi-repo-team.md`, chartered by Rjae 2026-09-10; the ThinkTech case — the same team on `thinktech` and `thinktech-api`) — six measurements and no code change: two throwaway repositories on the fs-adapter rig sharing one committed `.brigade.json`; the same on the live team with a throwaway checkout; telling repositories apart by `workspace_label` in `sessions`/`whoami`/the frame; the join affordance judged by measured friction (hand-copied file or a verb); the code paths that assume one checkout per team; the native cross-session workaround compared. Deliverable `docs/experiments/E6-multi-repo.md` with one of three recommendations (works as-is, docs only / one small verb / does not work because of file:line) | done | Opus | `docs/experiments/E6-multi-repo.md`, this commit. **Answer: works as-is, docs only.** Both halves run, not reasoned: the fs rig (two throwaway repos, one committed `.brigade.json`, secret-free join through the re-consent path, both sessions on one listing, delivery both ways, two pins one team) and the **live `brigade` team** (throwaway checkout joined secret-free, session registered, delivery both ways, torn down to a byte-identical `projects.json` with the credential store untouched — local cleanup only, no `team` verb, per the brief's corrected §6). Identification is solved for the **unrenamed case only, and not by Brigade**: the name resolves `entry.Name` → `session_title` → `basename(cwd)`, first non-empty wins (`hook/hook.go:666`), and in a real session `entry.Name` is always set, so Brigade's own fallback never fires — what makes a checkout legible is that **Claude Code** derives its session name as `<basename>-<suffix>` (measured: 8 `nameSource: derived` entries all of that shape, 15 `nameSource: user` typically unrelated to their checkout). A `/rename` therefore drops the repository, and the member that survives one, `workspace_label`, is opt-in and `--json`-only, never in the frame (`from-label` is always the human label, `frame.go:352`). Anything built on "the session name identifies the checkout" rests on a Claude Code convention Brigade neither sets nor controls. Corrected over two review rounds by `18-support-multi-repo-teams` on PR #7, which caught the first draft overstating this and the second attributing it to the wrong source. Docs to change, named in the report — **four targets, not three**: `docs/setup.md:99-103`, `plugin/README.md:132-133` (which currently points the wrong way), `docs/security.md:157`, and `docs/setup.md:336-343` ("Two commands end the credential"), which the report argues is **the more important of the two placements** for the no-checkout-scoped-undo consequence because it is where a reader who wants to detach one checkout actually looks. Four findings in §3.5, the sharpest being **no checkout-scoped undo verb** — found before the live run and already folded into the brief at `d94bfe4`. Two out-of-scope findings worth their own rows if wanted: `team create --adapter <path>` writes a path into `.brigade.json` that `team join` then refuses, and `config.RegisterAdapter`'s doc comment names a CLI writer (`profile init --adapter`) this build does not have; orphan pins are never pruned. Nothing ships; a docs change is a separate row Rjae charters |
| P11-2 | **`supabase link` broke upstream; `make backend-install` repaired to skip it** — found deploying `thinktech-brigade` on 2026-09-10: `link` reads the legacy `service_role` key through `GET /v1/projects/<ref>/api-keys?reveal=true`, Supabase no longer hands that reveal to a personal access token, and it dies on `LegacyLinkAuthTokenError` for **every** project and **every** token. Brigade never needed `link`: `db push` and `migration list` take `--project-ref`, and `db push` mints its own login role (`POST /v1/projects/<ref>/cli/login-role`) | done | Opus | this commit — `backend-install` drops `link` and passes `--project-ref` to all three commands; its `api-keys` line is filtered to the publishable key (it had been printing the legacy `service_role` JWT, against the CLAUDE.md rule, while its comment claimed "the publishable key only"); `backend_settings_test.go` pins both, `docs/setup.md` gains a creation-time section 0 and a five-step ordered path at the top of "Administrator: create a team", `scripts/backend-settings.sh`'s header drops the now-false `jwt_secret` claim, `docs/research/house-conventions.md:458` re-stated. **Measured:** the token that deployed `wmgtaraqmoufmrnyojzf` on 2026-09-05 fails `link` today against that same project, on CLI 2.116.0 **and** 2.117.0 — a platform change, not a token problem. `thinktech-brigade` (`hsopqzvznxajeyswzztv`, us-west-2) deployed end to end with a **project-scoped** token holding no organization access: five migrations `local == remote`, ten settings at target, second run zero PATCHes, keep-alive rungs `health 200, signup 200, rpc 200, logout 204`. `make test` 44 passed / 0 failed / 1 skipped; both shellchecks green |
| P11-3 | **Keep-alive for the second hosted project** (`thinktech-brigade`): `keepalive.yml` gains a `keepalive-thinktech` job with its own variable pair, so both Free-plan projects answer a few database requests a day. One job per project rather than a matrix over `vars[matrix.…]`: a dynamic lookup hides which project a red run belongs to, and separate jobs already fail independently | done | Opus | this commit — the second job, `BRIGADE_THINKTECH_SUPABASE_URL` and `BRIGADE_THINKTECH_SUPABASE_PUBLISHABLE_KEY` set on the repository 2026-09-10, `docs/setup.md` §2 rewritten as "The repository variables" with the second pair, `scripts/ci/README.md`'s workflow row / two new configuration rows / the `keepalive.sh` section, and `keepalive_test.go` pinning the job, its pair, the doc lines and a one-`run:`-per-job count so a third project cannot be added without its rungs |
| P11-4 | **One team, several repositories — the documentation P11-1 named** (Trello card 18, chartered by Rjae 2026-09-13): the same `.brigade.json` committed in several repositories is one team, joined once per checkout and secret-free on a machine that already holds the credential; the credential is per team per machine, so `team reset`, `team revoke-credentials` and `team leave` act for every checkout and no verb detaches one | done | Fable | landed in `6f16342` — swept into a concurrent session's card-20 commit by its whole-tree `make push` from the same checkout; this row's own commit carries only the row — `docs/setup.md`: "The project owns the team" gains the multi-repository paragraph with the two-step join (copy the file, `team join`); the member bullet re-cut from "several projects" to "same file, one team; different file, different team"; "Two commands end the credential" states the per-machine scope with `team leave` included and the only checkout-scoped undo (the pin in `projects.json`). The same wrong-way sentence corrected in its two other copies, `plugin/README.md` and `plugin/skills/setup/SKILL.md` — the study named the README only; `setup_docs_test.go` still holds the three copies to the same commands. `docs/security.md` §4 states the consequence beside the concession it already made. The question E6 §3.3 leaves open — `workspace_label` in the human `sessions` line — is not this row |
| P11-5 | **A session carries its repository's name, and the roster shows it** (Trello card 18, Rjae's ruling 2026-09-13 on the question E6 §3.3 left open: identification must not rest on a Claude Code naming convention or on nobody running `/rename`; zero configuration; the repository's name, not the folder's; the security promise sentence removed rather than re-argued) — `workspace_label` defaults to the repository name, `brigade sessions` prints it | done | Fable | this commit — `teamfile.RepoName` (the `origin` remote's last URL segment without `.git`, else the first remote's, else the toplevel directory's name; git's own files only — `.git/config`, a worktree's `gitdir:` pointer and `commondir` — each read capped, Lstat like `Toplevel`, sanitised as a label; tests for every URL spelling, origin-first, the fallbacks, a linked worktree, a hostile URL and an oversized config); `share_workspace_label` defaults to **on** (`config.Options`, plugin.json, README), the hook registers the user's label else the derived name (`start.go` `workspaceLabel`), the by-pid map carries it and the watcher's re-open sends it — a resume that omits it **clears it at the backend** (`session_model_context.sql:50`), which the reopen test now pins; `sessions` gains a `repo=` column beside the name only for sessions that registered one (an older harness's line keeps its shape), `whoami` a `repo:` line and a `workspace_label` member. Docs: plugin.json and README option rows, `docs/setup.md`'s multi-repository paragraph, the team-messaging skill's column list, `docs/security.md`'s "a label you choose, never a path" sentence removed (Rjae: documenting the option is enough), `docs/protocol-v1.md`'s `opt-in and user-typed` note dropped from the `workspace_label` row — a prose cell, not a wire shape; BAP/1 unchanged. CHANGELOG `[Unreleased]`. Frame unchanged (U-04): the roster is where a recipient is chosen. **Released as 0.6.0** the same day (`1c1dea5`, tag `v0.6.0`, release run 34775978678 green: four binaries and `checksums.txt` verified against the committed file). The release-notes gate (run 34776019499): lint clean, reviewer `CHANGES_REQUESTED` on round 2 with two true findings — the 0.6.0 CHANGELOG had no entry for `103ad8d`'s shipped watcher change (`attempt.go`: one heartbeat outstanding on the stdin path), and the Added entry read as though spanning repositories were new — both amended in `CHANGELOG.md` (`7f4b3ae`) and the gate re-dispatched on `v0.6.0`: run 34777016553 **approved**, the release page carries the written notes. `release-prep.sh` then took `card=<n>` in place of its pinned 15 (Rjae, 2026-09-13; `make release version=X.Y.Z card=<n>`, refused without one, `release_prep_test.go` proves the card is the one passed) |
| P11-6 | **`/brigade:sessions`: the roster gets a command, because asking for it in words does not print the same thing twice** (Trello card 19, Frank, 2026-09-11) — asking a session for the team's sessions ran `brigade sessions` and then rendered it however the turn saw fit: a table with a caveat one time, a table plus an unrequested "changed since last run" diff the next, in one session and across sessions. The CLI's output was identical both times; the variance was entirely in the model's rendering of it. A fifth user-invocable skill, a passthrough, with `disable-model-invocation: true` so the unprompted roster read stays with `brigade:team-messaging` | done | Opus | this commit — `plugin/skills/sessions/SKILL.md` (`argument-hint: "[--all]"`, `allowed-tools: Bash(brigade sessions:*)`, three rules: the argument is empty or `--all` and nothing else; stdout verbatim in a fence with no table, count, summary or cross-run comparison; one stderr line and stop on failure). **Measured before writing the grant:** a `<cmd> <sub>:*` Bash rule matches the BARE two-word command as well as an argumented one — a `Bash(echo probe:*)` deny rule in `claude -p --settings` denied `echo probe` and `echo probe here` alike (`permission_denials` carries each; both runs answered DENIED), so the single prefix pattern is the whole grant and no exact-form pattern is needed. **The name was load-bearing elsewhere:** `/brigade:sessions` was the repository's canonical example of a skill that does not exist, from the v0.5.0 notes slip (P9-8), and `TestReleaseNotesLintReadsTheSkillsFromThePluginTree` asserted the lint REFUSES it when read from the real `plugin/skills/` tree — so creating the skill turned that test red (seen red: `ok: /brigade:sessions exists`). Re-pointed at `/brigade:roster`, a name the plugin has never had, with the synthetic `skills` const and the table's skill refusal moved with it; the four prose sites that called the skill non-existent (`release-notes.yml`'s header and its writer prompt rule 4, `release-notes-lint.sh`'s header, `scripts/ci/README.md`) re-stated in the past tense without naming an unreleased version. `CHANGELOG.md` gains an `## [Unreleased]` entry; `plugin/README.md` four skills → five in both places; `docs/setup.md` documents it after the SessionStart line. No CI pin on which skills exist (the 2026-09-08 open-by-default ruling), no change to the CLI or its output. **Three blockers on PR #8**, all of them counting errors in the surfaces the change touched: the reworded rule 4 had dropped a sentence that was not this change's to drop ("Name a `brigade <verb>` ONLY if `/tmp/brigade-notes/brigade --help` lists that verb"), weakening the writer prompt's round-1 CLI grounding while the PR body claimed the hunk kept its check; `README.md:130`'s repository map still said "the four skills"; and `docs/security.md:263`, which enumerates the user-invocable skills shipping a Bash grant, named only `join` and `update`. The fixer agent took the second and third (`fac1ec2`, adding that the `sessions` grant is the narrowest of the three), the first was restored by hand. The branch was **stacked on PR #7** because local `master` sat one commit ahead of `origin/master` at `3928769`; left stacked, merge order noted on the PR (#7 first) rather than rebased. **A second, independent review** — asked of `brigade-53` on Rjae's machine over Brigade, which stood down from the duplicate work its session name implied (clean tree, nothing written) — returned seven findings, six of them acted on and every one verified against the tree first. The two that matter: **`CHANGELOG.md:48` still said the skill "does not exist" in the present tense**, 34 lines below the `## [Unreleased]` entry announcing it, and it is the only one of the five prose sites an end user reads and an input the release-notes gate points both its agents at (`release-notes.yml:80`, `:156`) — a false present-tense claim about the current tree is not history, so it took the same past-tense treatment as the other four; and **the skill's rules 2 and 3 were written around a stdout/stderr distinction the model cannot observe**, since the Bash tool hands it one text result, which `plugin/bin/brigade:158` makes concrete — the first use on a machine prints `brigade: first use: downloading …` to stderr and then execs, so a successful first run carries a non-roster line that rule 2 forbade printing and rule 3 did not cover. Both rules now turn on the exit status and on "what it printed", with the first-use line named. Also: the preamble asserted `$ARGUMENTS` "is empty or `--all`" as fact while rule 1 tested a third case; `--all` now matches ignoring surrounding whitespace; `docs/security.md` drops "one verb rather than the whole CLI", which was equally true of `update`'s `Bash(claude plugin:*)`, for the stronger true claim (one read-only verb, against every `brigade` verb and against installing and removing software); and the `/brigade:roster` sentinel is gone — `roster` is the name this repository's own prose uses for the roster feature (`plugin/README.md:27`, `docs/setup.md:195`, `CHANGELOG.md:14`, this row's own title), so it was the next `sessions`, and `absentSkill(t)` now derives a name the checkout does not carry instead of hard-coding one. The seventh finding, that `Bash(brigade sessions:*)` prefix-matches the `--json` form rule 1 forbids in prose, was checked and wanted no change: `sessions.go` sanitises the same record either way. Finding 5 is the one that widened the change — the prompt that motivated this row ("brigade sessions" typed as words) reaches `brigade:team-messaging`, not this skill, whose `disable-model-invocation: true` is correct, so the reported defect still reproduced for anyone asking in words; team-messaging gains one rendering rule scoped to *showing* the roster, which leaves the model free when it reads the roster to address a message. **A third round on the same review** caught two consequences of that widening, both the CHANGELOG-48 class — a prose site left standing by a change made after it — and one of them written in this PR's own first commit: `docs/setup.md:198` still said asking in words gets the roster "formatted however that turn's session saw fit", which `2a5b4a8` made false and which the new changelog bullet contradicts outright, so the user-facing document and the changelog entry disagreed inside one PR (now states that team-messaging carries the rule too, while being honest that it is one rule in a skill the session may not have in play, and that only the slash command is bound to the passthrough); and the new rule said "the roster", a word `team-messaging` already uses six lines above at `:48` to label `brigade team members`, so a model showing that command had to guess whether the rule governed it — the rule now names `brigade sessions` and `brigade team members` explicitly and covers both, being the same failure mode on the same kind of output. `absentSkill`'s first candidate is the bare `no-such-skill`, so one spelling serves both tests. A fourth round found the **fourth instance of the same class, in the same file as the first**: the `## [Unreleased]` bullet added for the team-messaging rule was itself never revisited, so it promised the steadier prose path with no caveat while `docs/setup.md` hedged it and `docs/security.md` measures the skill as **never loaded once in 98 interactive sessions** (`:126`, `:270`) — three documents, three different claims, and the changelog is the one that ships to the release page — and it described the pre-widening rule, "showing you the roster", the exact word removed from the rule itself for being `brigade team members`'s label six lines above. Rewritten with both commands named and the not-identical caveat. The review also settled that `setup.md`'s hedge **understates** rather than overstates: the house measurement is stronger than "may not have it in play", so the hedge stands unchanged and the measurement stays out of the getting-started document. **The class, named plainly since it cost four fixes here:** every instance is a *description of a rule living in a different file from the rule*, written before the rule changed. Nothing mechanical catches it — `release-notes-lint.sh` checks that names exist, not that claims are true, and the 2026-09-08 open-by-default ruling keeps CI out of prose — so the only control is grepping for every place that describes a behaviour before changing the behaviour. Four instances in one change argues for that habit, not for a new gate. **A fifth instance arrived by a different mechanism** and is worth separating from the other four: `docs/claude-code-usage.md` — the usage guide `CLAUDE.md` designates, which tells the next agent what the plugin ships — enumerates the plugin's skills, and it **did not exist when this change was written**; it landed on master in `6f16342`/`678ed47` two days later, already naming four. So grepping before changing the behaviour could not have caught it: the description was written after the rule. The control for this one is re-grepping the enumeration surfaces **after every merge of master**, not only before the change — a long-lived branch acquires descriptions of itself that it never wrote. Found by the repository reviewer on `293e178` and confirmed by an independent sweep of every file naming two or more `/brigade:` skills; `docs/experiments/E5-release.md:956` (0.4.0's "two skills") and the log archive are historical and correctly untouched. **A sixth instance, by a third mechanism: a stale *pronoun*, not a stale description.** The `docs/security.md` §5 paragraph this change edited ends on the house measurement — 98 interactive sessions that saw a hostile message, "**the skill was never loaded once**", "Do not plan around it" — whose subject was `brigade:team-messaging` when it was written (§3, `:125`). Inserting two sentences about the `sessions` grant in front of it moved the nearest antecedent of both "Declaring it" and "the skill" onto the new skill, where the claim is false by construction (a `disable-model-invocation: true`, user-invoked-only skill that did not exist when those sessions ran) and the advice would read as *do not rely on the deterministic path this change ships* — in the one document where an ambiguous subject costs a wrong conclusion about the posture. Grepping the descriptions of a behaviour cannot catch this: nothing described anything wrongly, an insertion re-pointed a pronoun. The control is that **a pronoun near an insertion point is an edit to the pronoun**: name the subject whenever text lands between it and its referent. Fixed by naming both subjects (`Declaring allowed-tools`, `brigade:team-messaging was never loaded once`) and splitting the paragraph so the measurement stays contiguous with the skill it measures, with the three user-invocable grants in their own paragraph saying outright that the §3 measurement says nothing about a skill the model cannot load. Editing `docs/security.md` is otherwise off-limits to the fixer agent (`developer.md`); the review asked for it by file and line and gave the reason, which is the ask that rule wants **Landed and released (Rjae's word, 2026-09-13).** Master moved under the branch three more times during the day (`7f4b3ae`, `771f5de`, the 0.6.0 release); each merged in, the last by `brigade-53` from an isolated worktree once Rjae asked it to land the PR (`cc29596`: master's P11-5 text kept, this row beneath it). The reviewer's run on that head posted one real finding that three prior review rounds had missed — inserting the two `sessions` sentences in `docs/security.md` made "the skill was never loaded once" read as a claim about `sessions` — and `claude.yml`'s fixer pushed `3f333fb` (the measurement re-attached to `brigade:team-messaging` by name, the three grants in their own paragraph, no claim changed). **Three mechanics of the loop, measured on this PR:** (1) a `claude[bot]` push holds its workflows at `action_required` (`fac1ec2` on the 11th, `3f333fb` today), so the loop cannot close a review round without a human approving the ci run — approved ci only, never the reviewer's; (2) the reviewer's `--max-turns 30` (`review-pull-request.yml:126`, against 50 in `release-notes.yml` and 60 in `claude.yml`) died three times on this branch (988a92b no verdict; 293e178 and cc29596 a verdict, then a red job at 31–32 turns), each stale `CHANGES_REQUESTED` dismissed with the fixing commit named; (3) GitHub's `mergeStateStatus` lagged the re-run of a red `fast` and refused a direct squash with every required check green, so `gh pr merge --squash --auto` was armed and GitHub merged on its own recompute. That red `fast` (run 34778172498 attempt 1, job 103780045371, shuffle seed 1789328254153052106) is a survivor of `103ad8d`'s sweep: `TestReleaseSurvivesAWatcherRestart` passed every assertion and failed in `t.TempDir()`'s RemoveAll with `brigade-state: directory not empty` — a writer still in the state directory after `watch.Run` had returned 0, on code byte-identical to master's; card 20's, untouched here. Merged as **`2731453`** (`19: Add /brigade:sessions, a verbatim passthrough for the roster (#8)`, retitled from `15:` under the `<card>:` rule; Trello card 19 verified from the card). **Released as 0.6.1 the same hour:** `b340791` names the section, `37941b8` `19: Release 0.6.1`, tag `v0.6.1` published 20:09:36Z with the four binaries and `checksums.txt`, goreleaser byte-identical to `make cross`, `make test` 45/0/1 on both pushes; on `37941b8` all four of master's jobs green — `fast` (checksums-check), `macos`, `reproducibility`, `supabase`. **The notes gate passed in round 1** (run 34779894742): `ok: /brigade:sessions exists` read from the tag's `plugin/skills/`, lint clean, reviewer APPROVED, and the published notes lead with the skill — the v0.5.0 slip's name, now real, is the first thing the gate proved from the tree. **One defect in the release path, found here:** `release.yml`'s single `gh workflow run` produced **two** `workflow_dispatch` runs in the same second (34779894653 and 34779894742, both `github-actions[bot]`); both ran the whole gate, both published the same notes (idempotent), and both opened the announcement issue, because the step's `gh issue list --search` guard cannot see an issue created one second earlier (#10 and #11, byte-identical; #11 closed as the duplicate). A concurrency group keyed on the tag, or a list-based guard, would close it; not this row's to fix |
| P11-7 | **`/brigade:update` moved the wrong install; how Claude Code picks a plugin version, measured** (Trello card 23, Rjae, 2026-09-14) — Rjae, with three clones of `GoThinkTech/thinktech` under two Claude Code accounts, saw a folder stay on 0.3.0 after an update and had never seen the collaborator install offer `docs/setup.md` described. Measured in a spare account with `claude -p` sessions (`docs/experiments/E8-plugin-scope.md`, Claude Code 2.1.270): the install RECORDS decide the loaded version, not the cache (every record aged to 0.4.1 with 0.6.1 cached loaded 0.4.1 everywhere); a user-scope record wins in every folder even over a newer project-scope record (user 0.4.1 / project 0.6.1 loaded 0.4.1); with no user record each folder loads its own project record; a committed `enabledPlugins` creates an inert per-folder record on first open and installs nothing; a running session keeps its version until `/reload-plugins`. A first run inherited this session's PATH and answered every arm from its own 0.6.1 bootstrap — retracted, and re-run with a clean PATH behind a negative control (`command not found` with nothing installed) | done | Fable | this commit — `plugin/skills/update/SKILL.md` updates `--scope user` first, project only when user is not installed, and says which folders follow; `internal/harness/watch/classify.go`'s `config` notice points at `brigade whoami` (the `profile` verb left in 0.4.0; the test pin moved with it); `docs/setup.md`'s install section answers the scope question with **user**, gives the collaborator the one-command path, and says what a committed `enabledPlugins` does and does not do; `docs/experiments/E8-plugin-scope.md` with its driver `scripts/experiments/E8-plugin-scope/run-session.sh`; `CHANGELOG.md` `[Unreleased]`; released as 0.6.2: `516b7db` names the section, `32f61a9` `23: Release 0.6.2`, tag `v0.6.2` published 2026-09-14T13:51:40Z with the four binaries and `checksums.txt`, goreleaser byte-identical to `make cross`, `make test` 45/0/1 on all three pushes; on `32f61a9` all four of master's jobs green — `fast` (checksums-check), `macos`, `reproducibility`, `supabase`. **The notes gate passed in round 2** (run 34851874828): round 1's lint refused `brigade profile` as a CLI verb — the writer had kept the changelog's own backticked `brigade profile status`, the verb this very change removes, so the refusal was correct by the letter and the fix cycle reworded it; round 2 lint clean, reviewer APPROVED, one announcement issue (#12). **One dispatch this time** — the double dispatch of 0.6.1 (card 22) did not recur, which says it is intermittent, not structural. Rjae's chosen configuration: user scope per account, committed marketplace kept; the three thinktech clones' project records and pins were removed in both accounts ahead of that install. **Docs follow-up, 0.6.3:** Rjae ran the whole matrix (two accounts × three clones) and asked for the procedures written as he had them confirmed — install once per account then once per clone, update once per account — so `docs/setup.md` gains the two step lists and its update sentence, and `plugin/README.md`'s, say one `/brigade:update` per account moves every folder; released as 0.6.3 for him to test: `64f8ec9` the docs, `ff3ce12` names the section, `95dc26f` `23: Release 0.6.3`, tag `v0.6.3` published 2026-09-14T15:16:55Z with the four binaries and `checksums.txt`, goreleaser byte-identical to `make cross`, `make test` 45/0/1 on all three pushes, all four of master's jobs green on `95dc26f`; notes gate (run 34861132506) passed in round 2 — round 1 lint clean, reviewer CHANGES_REQUESTED on content, fixed and APPROVED — one dispatch, one announcement issue (#13). Two releases in a row with a single dispatch: 0.6.1's double dispatch is intermittent. Setting up a non-technical member, Rjae asked where the marketplace step was: the Install list had leaned on the committed marketplace being added on trust (unmeasured), so the list briefly opened with `/plugin marketplace add appshapes/brigade` (`19d832e`) — then Rjae measured on the founder's never-seen-Brigade machine that the committed marketplace IS added on trust and `/plugin install brigade@brigade` alone installs, so the step came out again the same day and the on-trust claim in `docs/setup.md` and E8 is now stated as measured — docs only, no release |
| P11-8 | **Install and update, settled against Anthropic's guidance and re-measured** (Trello card 23, Rjae, 2026-09-14) — after a day of circles Rjae restated the goal: simple to install, simple to upgrade, for non-technical users; security is the harness's job. A `claude-code-guide` agent fetched the documentation: project scope *is* the committed `enabledPlugins` entry ("install for all collaborators on this repository, which adds the plugin to `.claude/settings.json`"); settings precedence is project over user; a committed marketplace is added on trust without a prompt; marketplaces can `autoUpdate` (background, interactive, then a `/reload-plugins` prompt; off by default for third-party marketplaces). Measured in a spare account with trusted folders (`docs/experiments/E9-plugin-install-update.md`): a repository committing the marketplace only → first trusted open adds the marketplace and installs nothing; one `claude plugin install --scope user` → all three thinktech clones load the one version; an older user record → one `update --scope user` → all three clones follow; a committed `"autoUpdate": true` propagates into the collaborator's `known_marketplaces.json`. **The `-p` rig cannot observe interactive precedence**: trusted folder, project 0.6.1 / user 0.6.3, `-p` loaded 0.6.3 while Rjae's interactive `df064357` loaded the project record — E8's conclusion 2 is superseded; the recommended configuration never creates two records, so the point is moot for users | done | Fable | this commit — this repository's `.claude/settings.json` becomes marketplace-only with `autoUpdate: true` (the project-scope record it had created in `~/.claude` was removed; `claude plugin uninstall --scope project` edits the committed file, which is how the two thinktech clones had gone dirty); `docs/setup.md` install section and the two procedures (Update: automatic, `/brigade:update` for now); `plugin/README.md`; `plugin/skills/update/SKILL.md` updates user then project and treats `not installed` as information; E8 marked superseded in part; E9 report; released as 0.6.4: `6bfb6ee` the change, `6e84fa9` names the section, `e1a87ef` `23: Release 0.6.4`, tag `v0.6.4` published 2026-09-14T19:18:17Z with the four binaries and `checksums.txt`, goreleaser byte-identical to `make cross`, `make test` 45/0/1 on all three pushes, all four of master's jobs green on `e1a87ef`; notes gate (run 34886073617) passed in round 2 — round 1 lint clean, reviewer CHANGES_REQUESTED on content, fixed and APPROVED — one dispatch, one announcement issue (#14). **End state applied:** `~/.claude` and `~/.claude-thinktech` each hold one user-scope record at 0.6.4 and the `brigade` marketplace with `autoUpdate: true`; the three thinktech clones load 0.6.4 under the account that can start sessions, with nothing run in them; all three are pinned to the team. Closing: `GoThinkTech/thinktech` committed `"autoUpdate": true` on its marketplace entry (`fb89201d0`, card 17, 2026-09-15), so its collaborators inherit background updates; two `expect`-driven interactive sessions in the `tt-enabled` scratch clone tried to repeat the precedence observation and were inconclusive (the TUI's input was never seen executing; Claude Code moved 2.1.270 → 2.1.272 between them), so `df064357` stands as the single interactive observation and the scratch clone, its 0.6.1 record and its trust entry were removed; `~/.claude` verified by Rjae from a live session in `thinktech-php` on 2026-09-15: `brigade version` → 0.6.4, session attached to team `thinktech`, no project record created by the open. **Both accounts and all three clones on one configuration: a single user-scope install at 0.6.4, marketplace auto-update on, every clone pinned.** **Landing the docs (Rjae, 2026-09-15):** the front `README.md` rewritten for the non-technical member — six journeys in Rjae's order (install, join, update-automatic, create team, add a repository, create the organization's Supabase project), no commentary, no mention of several accounts — with the developer sections moved verbatim to the new `docs/development.md` and `docs/setup.md` given an explicit Updates section for administrators (auto-update, `/brigade:update`, one account per install) |
| P12-1 | **Backend upgrade from the plugin alone — the four P0 measurements** of `.context/plans/backend-upgrade.md` §4: the Management API query endpoint running a whole migration file (multiple statements, `$$` bodies, create/drop function), its limits; whether `db push --dry-run` accepts a Brigade-recorded `schema_migrations` row; a project-scoped token; PostgREST's schema cache after such an apply. Recorded in `docs/experiments/E7-backend-upgrade.md` | todo | Opus | not started — **awaiting Rjae's charter of the brief**; measured so far (2026-09-10): the query endpoint answers read-only SQL with a PAT; `schema_migrations` is `(version text, statements text[], name text)` and the CLI compares versions only; `db push` uses a minted login role and a direct Postgres connection, which the binary cannot (no driver in `docs/allowed-deps.txt`) |
| P12-2 | The `brigade adapter supabase backend upgrade --project-ref <ref> --token-file <path> [--dry-run]` verb: migrations embedded in the binary with an embed-check gate, the query-endpoint client, pending computation, applier, bookkeeping row, the secret handled like the join secret (file or stdin, never argv), tests against a fake Management API (brief §3, §5) | todo | Fable | not started; follows P12-1 |
| P13-1 | **Intermittent `make test` failures fixed at their mechanisms or removed** (ticket 20; Rjae: "either permanently fix these failures, or remove them from the suite … I would rather lose test coverage than continue along with intermittent failures"): ETXTBSY on Linux (runs 34232764450, 34355720402, 34516308749, 34537042672 — the last one misread in `v0.1.0-followups.md` as a lease-clock flake), macOS's first-exec assessment of freshly written fakes stretching every 5 s wait in conformance and adapterkit, the watcher's stdin heartbeat backlog under an F_FULLFSYNC storm (closed_at never written; a `message send` starved past the 10 s lock timeout), dedup verdicts that depended on Offer→Done latency against the fake's replay gap (34622839499, 34615698541), and every real-clock performance bound left in the tests | done | Fable | this commit — `internal/testutil.WriteExecutable` writes every executable fixture as a new inode under `syscall.ForkLock` (a go/parser witness refuses any other executable write; the Linux stress test fails on the plain-WriteFile mutant at 13–30 of 1,000), `cmd/brigade`'s testscript Setup re-materialises the txtar scripts the same way, and the conformance and adapterkit fakes are launched as `/bin/sh <script>` (viaShell, childArgv) so nothing execs a fresh file. **One production change**, `internal/harness/watch/attempt.go`: one stdin heartbeat outstanding at a time — a request due while the last is unanswered is deferred and sent on the answer, or with the next request once the unanswered one is older than the lease less 1.5 intervals (the 60 s tick, never the 90 s one). The bootstrap hook's timed hold and the supabase fake's join delay became held gates; every positive wait is a 30–60 s hang catcher. **Removed assertions, coverage lost on purpose:** the fixture-lease control arm; `TestConformanceFSWholeRun`'s 120 s ceiling; the flock median (< 50 ms), the same-process 2 s / 1 s and two-process +2.5 s stopwatches (the holder is now released by the parent after the bound fires; the race children rendezvous and loop until they have contended); `TestSpawnOverflowCancelsRunawayChild`'s 10 s and `TestSpawnWaitDelayGrandchildResultStands`'s 15 s upper bounds (the grandchild's liveness is the witness); `TestExitsAfterClaudeDeath`'s 5 s latency; the `== 2 duplicate` counts in the two dedup tests (ack iff duplicate now); the 700 ms hint / 2 s revocation / phx-leave exit bounds and the 5 s live-timer window in the supabase tests (construction witnesses and a constant pin, `TestWatchTimingIsPlan56`); the 200 ms sleep in `TestWatchExitRangeAndSignals`; and e2e's bare `closed_at` after session-end — both closers keep their 1 s production budgets (`adapterclient.CloseTimeout`, `CloseWaitClean`), so the hook's `close did not complete … deadline` line is the tolerated alternative. **Already fixed at HEAD, recorded only:** bootstrap's `a download temp file survived` (1c849bd unlinks `$tmp` before the marker) and procutil's `read /proc/<pid>/stat: no such process` (f450d2d maps a read-time ESRCH to gone). **Out of scope:** `TestKeepaliveWorkflowAndDocsAgree` (a real doc break, 33996691820) and `supabase start`'s port bind (33979534619, runner infrastructure; recommendation: one retry of that step with `make supabase-clean` and a port-holder dump between attempts, never a retry on a go test step) |
| P13-2 | **Trello skills and the Claude Code usage guide**, ported from `thinktech-php` commits `f283cb3` and `28fa4f3` (Rjae, 2026-09-13): `/trello-create` and `/trello-read` with the AppShapes board (`65be2d49d367b88f7e35ab55`) and the `Wanting` list as defaults, `docs/claude-code-usage.md` (agents, skills, the daily workflow, the Trello CLI setup, a command reference) linked from the README's table of contents and Layout, the skills recorded in `CLAUDE.md`, and the commit-prefix rule now saying where the number comes from (an AppShapes card; 15 is the build-out card, 20 the intermittent-failures card) | done | Fable | this commit; the `supabase start` port-bind retry recommended in P13-1 is card 21 on the AppShapes board |
| P14-1 | **The PR reviewer's turn budget, measured and re-pinned; the verify step decides the job, not the action** (Trello card 22 "Workflow fixes", item 1; 2026-09-16) — `review-pull-request.yml` ran the reviewer at the provisional `--max-turns 30` (against 50 in `release-notes.yml`, raised in `d684bca` for exactly this, and 60 in `claude.yml`), and on PR #8 the branch outgrew it: every ceiling death left a stale `CHANGES_REQUESTED` or nothing, each dismissed by hand. This is the re-pin that §7.3 of the agentic-workflows brief and the P9-3 row deferred to "the first ten real runs" | done | Fable | this commit — **measured 2026-09-16 over all 17 runs of the workflow** (`gh run list`, `gh run view --json jobs` for the job durations, the run logs' `num_turns`/`subtype` and the action's error line, the PR reviews matched by `commit_id`): 15 reached a job, 2 were held at `action_required` and never ran (`fac1ec2`, `3f333fb`), 14 reported a turn count — 16, 18, 19, 24, 24, 25, 28, 30, 31, 31, 32, 33, 40, 42 — the largest **42** (run 34618438152, `492e947`, PR #8), and the six past 30 were two failure modes, not one: four SUBMITTED a verdict and were then failed by the action for exceeding the maximum (runs 34614440349 at 33, 34618438152 at 42, 34776879430 at 32, 34778172505 at 40), two stopped at 31 with `error_max_turns` and no verdict (runs 34613414340, 34623412660). A seventh, run 34615412734 (30 turns, `56609fb`), submitted its review after the head had moved to `029cd34` and the verify step failed loud for the old head — not a ceiling death; recorded for completeness. Longest job 5m41s (run 34778172505); all fifteen inside six minutes. **Chosen:** `--max-turns 70`, by the rule "at least 1.5 × the largest observed turn count, rounded up to the next ten" (1.5 × 42 = 63; neither 50 nor 60 clears it, so this budget matches neither of the other two workflows, and the file says why; re-measure when a session reaches 47); `timeout-minutes: 30` kept by ci.yml's rule (at least 2 × the longest observed job, 11m22s — 30 also covers a full 70-turn session at the slowest observed ~8.5 s/turn twice over). **`continue-on-error: true` on the action step**, the `d684bca` shape: "Verify a review was submitted" is now the job's verdict — it runs after a continued failure without `if: always()` (the job's status stays green, so the default `success()` holds), a claude review at `HEAD_SHA` passes and none fails; the step gains `id: review` and the verify step's error line carries `steps.review.outcome`, so a red job says whether the agent step failed or ran clean and never submitted. The stale "NOT a measured p90 (no runs exist)" comment is replaced by the measured one, dated, naming the runs and the rules; the historical workflow copies in `agentic-workflows.md` are untouched by design. **Correction to the P11-6 row** (left as written): it records cc29596's death as "a verdict, then a red job at 31–32 turns"; run 34778172505 reports **40** turns (32 is 293e178, run 34776879430). No `CHANGELOG.md` entry: `d684bca`, the precedent workflow-only budget change, carried none. Validated: YAML parse (ruby), actionlint (`rhysd/actionlint:latest` in Docker) clean but for its pre-existing `runner-label` notice on the Blacksmith label (line 32, untouched; the same notice on HEAD), `make typecheck pull build test` green before the commit. |
| P14-2 | **The fixer's pushes no longer wait for a human: the fork-PR approval policy gates only accounts new to GitHub** (Trello card 22 "Workflow fixes", item 2; Rjae's ruling 2026-09-16: "let's make the changes so that this stuff runs and finishes autonomously") — a fixer push (the Claude App token's `synchronize`) held `ci` and `review pr` at `action_required` on `029cd34` (PR #7), `fac1ec2` and `3f333fb` (PR #8), so no review round could close without a human approving the held runs; on PR #8 only `ci` was approved, the reviewer's run 34778601684 never executed, and the head that merged was never reviewed. Why here and never in the owner's other repositories: GitHub's policy "applies to pull requests triggered by either the pull request author or the actor of the pull request event" and exists only on public repositories (the API answers `Fork PR approval is not allowed for private repositories` for `brigade-adapter-kafka`, `drbl-app` and `drbl-api`); `brigade` is the organization's one public repository, and under `first_time_contributors` the bot never qualifies because every merge is a squash commit authored by the owner, so it is a first-time contributor forever. The first-runs doc named `first_time_contributors_new_to_github` as the remaining step on 2026-09-10 and left "whether the bot is exempt" to the next fixer push, which answered it (not exempt) without the answer being recorded | done | Fable | this commit — **setting changed 2026-09-16** with `gh api -X PUT …/actions/permissions/fork-pr-contributor-approval -f approval_policy=first_time_contributors_new_to_github`, at the repository and at the organization (both read `first_time_contributors` before and `first_time_contributors_new_to_github` after; the organization value is the default for its public repositories, so both were set to leave no ambiguity). What it exposes: an outside account that is not new on GitHub may now run `ci.yml` on a pull request, forks included, without approval — fork runs receive no secrets, `deploy-staging` runs only on `master`, the reviewer refuses fork heads (`head.repo.full_name == github.repository`) and the fixer runs only on the reviewer's verdict, so the exposure is Blacksmith minutes; new accounts stay gated. Recorded in `scripts/ci/README.md` (Required GitHub configuration, a `Setting` row), `docs/experiments/agentic-workflows-first-runs.md` finding 1 (the open question answered, dated) and `docs/claude-code-usage.md` (the loop sentence now names the setting it rests on). **Measurement pending:** GitHub does not document whether a bot account counts as "new to GitHub"; the next fixer push on a same-repository PR decides — its `ci` and `review pr` runs should start without approval. If they hold again, the fallbacks are the card's other two remedies: a repository-scoped machine-user identity for the fixer (the classic PAT stays out of the agent job, `claude.yml`'s boundary) or documenting the human step; neither was applied. Reversal is the same call with `first_time_contributors`. |
| P14-3 | **The release and notes workflows survive a duplicated tag push; a published release is never deleted by its own cleanup** (Trello card 22 "Workflow fixes", item 3; 2026-09-16) — the card wrote "release.yml can dispatch the notes gate twice, and the announcement guard cannot survive it": one `gh workflow run release-notes.yml` producing two `workflow_dispatch` runs in the same second (34779894653 and 34779894742), both publishing the notes and both opening the announcement issue because `gh issue list --search` cannot see an issue created a second earlier (#10, #11). The run records, re-read 2026-09-16 with read-only `gh`, say otherwise: the single `git push origin v0.6.1` of `scripts/release-prep.sh:163` (one push; no `push.followTags`) started TWO `release.yml` runs, 34779853757 (created 2026-09-13T20:08:49Z) and 34779854172 (20:08:50Z), both `push`, same head 37941b8, actor Rjae, different check suites (94191345353, 94191346251), while the branch push of the same commit started one `ci` run and every other tag in the repository's history (16 release runs on 15 tags) started one. Each run ran every step (the jobs API's step timings): each goreleaser created its own draft — 388019994 and 388019992, five assets each — each ran `gh release edit v0.6.1 --draft=false --latest` at 20:09:35–36Z (the one that got there first published the draft gh's by-tag fallback found, 388019992, `published_at` 20:09:36Z, and the other's edit resolved the tag to that now-published release and re-edited it), and each ran "Dispatch the release notes" exactly once at 20:09:36Z. So the two notes runs (both created 20:09:38Z) are one dispatch per release run, not a double dispatch by one step; they ran in lockstep — "Announce, once" at 20:12:58–59Z and 20:12:59–20:13:00Z, #10 created 20:12:59Z, #11 at 20:13:00Z — one second apart, where no guard, search-backed or not, could have seen the other's issue without serialisation. The P11-6 and P11-7 rows carry the card's reading ("release.yml's single `gh workflow run` produced two `workflow_dispatch` runs"; "the double dispatch of 0.6.1 is intermittent") and are left as written; this row corrects them. The card names GitHub's duplicate delivery nowhere, but `claude.yml:103` already states "GitHub can deliver the same webhook twice" and builds its staleness guard on it; the same at-least-once assumption holds for the push that starts `release.yml`. Two further findings: draft 388019994 (name v0.6.1, five assets) was STILL an unpublished draft on the repository three days later; and, independent of duplicates, "Discard the draft" was `if: failure()` → `gh release delete "$GITHUB_REF_NAME" --yes`, which resolves the tag to the PUBLISHED release when one exists, so any failure after the Publish step (the dispatch, say) would have deleted the release just published, assets included. 0.6.2, 0.6.3 and 0.6.4 (2026-09-14) each had one release run and one notes run: the duplicate is intermittent | done | Fable | this commit — **`release.yml`, three changes:** (1) a top-level `concurrency` group `release-${{ github.ref }}`, `cancel-in-progress: false` — cancelling the run in flight would strand its draft and end it red (`if: failure()` does not fire on cancellation), cancelling the queued one would leave a red run on a tag that released fine; (2) a step "Is this tag already published?" (`id: published`) right after checkout: `gh api repos/$R/releases/tags/$TAG --jq .draft` — the tag endpoint answers for published releases only (a draft is 404 by tag), so 200 with `draft == false` writes `exists=true`, prints that the run is a duplicate delivery and does nothing else, and 404 or any API error writes `exists=false`; not `gh release view <tag>`, which falls back to a draft of that name when no published release matches. Every later step (setup-go, Guard, goreleaser, Verify, Publish, Dispatch; `success() &&` kept where it was) is gated on `steps.published.outputs.exists != 'true'`, so the second delivery ends green with a checkout and one GET. Why the duplicate must stop BEFORE goreleaser and not merely be serialised: goreleaser v2.18.0's `findRelease` uses GetReleaseByTag (published only), `createOrUpdateRelease` keeps the published state, and with `replace_existing_artifacts` unset (`.goreleaser.yaml` sets `mode: keep-existing` and nothing else) every asset upload gets HTTP 422 `already_exists` and returns Unrecoverable — a queued duplicate that reached goreleaser would fail red; (3) "Discard the draft" is now `if: failure() && steps.published.outputs.exists != 'true'` and deletes drafts by id: `gh api "repos/$R/releases?per_page=100"` with a jq filter that selects `.draft == true and .tag_name == env.TAG` and emits each `.id`, read by a loop that runs `gh api -X DELETE repos/$R/releases/<id>` on it, the listing and each delete tolerant of failure (cleanup never masks the failure that brought the step there) — never a release by tag. Header rewritten: the stale "only run … D1 rehearsal" sentence replaced by the sixteen-runs record and a paragraph on the duplicate delivery; every other comment kept. **`release-notes.yml`, two changes:** (4) `concurrency` group `release-notes-${{ inputs.tag }}`, `cancel-in-progress: false` (the `inputs` context is valid in `concurrency`; actionlint accepts it) — queued because the gate's writes are idempotent and a cancelled run would strand nothing but leave a red run; (5) "Announce, once" lists instead of searching: `gh api "repos/$R/issues?state=all&creator=github-actions%5Bbot%5D&per_page=100"` with a jq filter that drops every entry carrying a `.pull_request` (the issues endpoint lists pull requests too) and counts those whose `.title` equals `env.TITLE`, the title through `env`, newest first so the tag's own issue is on the first page; an API error now fails the step loud (`set -e` on the assignment) instead of falling through to a second issue as the old pipeline into `grep -q` did. The header's "a re-dispatch rewrites the same notes and opens no second issue" and the banner's "idempotent" are corrected to "idempotent under serialisation", with v0.6.1's two simultaneous runs stated, not deleted. **Considered and not taken:** skipping the whole gate when the issue exists — the header documents a re-dispatch that rewrites the same notes as a legitimate path and P9-8's fix cycle relies on it, so an existing issue skips the announcement only; `cancel-in-progress: true` (above); distinguishing 404 from other API errors in the published check (either way the run proceeds as a first run, and if it was in fact a duplicate goreleaser's 422 fails it red with the published release untouched, the discard step being drafts-only). **Rehearsed read-only against the live repository before the commit**, the steps' shell verbatim with a scratch `$GITHUB_OUTPUT`: the published check for `v0.6.4` → 200, draft false, `exists=true`, the duplicate-delivery line printed; for `v0.0.0-nonexistent` → `gh: Not Found (HTTP 404)`, `exists=false`, step exit 0; for `v0.6.1` → 388019992, `exists=true`. The draft listing for `TAG=v0.6.1` → exactly `388019994`; for `TAG=v0.6.4` → nothing (the loop body echoed instead of deleting). The announcement listing for "0.6.1 is out — run /brigade:update" → 2 (#10 open, #11 closed by Rjae at 20:14:36Z), for "9.9.9 is out — run /brigade:update" → 0; the bot's issues are #6 and #9–#14, no pull request among them. **The one approved write, 2026-09-16:** before, `388019994,true,"v0.6.1","v0.6.1",5` and `388019992,false,"v0.6.1","v0.6.1",5`; `gh api -X DELETE repos/appshapes/brigade/releases/388019994` exit 0; after, 388019994 → 404, 388019992 `draft false` with its five assets (`brigade_0.6.1_{darwin,linux}_{amd64,arm64}`, `checksums.txt`), `gh release view v0.6.1` → `isDraft false`, the only release tagged v0.6.1; 13 releases and 0 drafts on the repository. No `CHANGELOG.md` entry (precedent `d684bca`, workflow-only). Validated: YAML parse (ruby), actionlint (`rhysd/actionlint:latest` in Docker) clean but for its pre-existing `runner-label` notice on the Blacksmith label in both files (a pre-existing SC2016 info on the untouched `printf` body line, whose backticks are literal Markdown, is now silenced by an explained directive); `make typecheck pull build test` green before the commit. **No end-to-end test exists short of a release:** the next `make release` is the measurement — one green release run (two if GitHub duplicates the push again, the second ending green at "Is this tag already published?" with every later step skipped), one notes run, one announcement issue, no draft left behind. **Measured 2026-09-17 on v0.6.5** (`make release version=0.6.5 card=22`, release commit `bc27cac`): one release run (35221994939; "Is this tag already published?" answered `exists=false` and let the build through; Guard, goreleaser, Verify, Publish, Dispatch all green; Discard skipped), one notes run (35222103139; round-1 review, one fix cycle, round-2 approval, published), one announcement (#18), zero drafts on the repository. GitHub delivered the tag push once this time, so the duplicate no-op path remains exercised only by the rehearsal. |
| P14-4 | **The loop mints a PR title with the issue's own card prefix** (Trello card 22 "Workflow fixes", the prerequisite for item 4 to land unattended; 2026-09-16) — `claude.yml`'s open-pr step kept only a literal `15: ` prefix and minted every other title `15: <title>`, so under the `<card>:` rule (CLAUDE.md) a card's PR had to be retitled by hand before its squash merge (PR #8, `15:` → `19:`, the P11-6 row); item 4 goes through the loop as a `22:`-titled issue with `auto-merge`, and a hand retitle is exactly the human step items 1–3 removed | done | Fable | this commit — the `case` keeps any title of the form `<digits>: …` and prefixes `15: ` only to an unprefixed one (the scheduled callers' maintenance work orders, the umbrella card); the comment there, `auto-merge.yml`'s two comment lines, `.claude/agents/developer.md` (the PR title and the commit-message lines) and `.claude/agents/reviewer.md` (the message-format line) say `<card>:` instead of `15:`; `_create-claude-issue.yml`'s note stays, its titles are unprefixed by design. Validated: YAML parse (ruby), actionlint in Docker (only the known `runner-label` notice), `make typecheck pull build test`. Measured by item 4's PR: its title must read `22: …` as minted. |
| P14-5 | **The developer's turn budget, measured and re-pinned; a branch pushed before the ceiling still becomes a PR, and a dead agent no longer closes its issue** (Trello card 22 "Workflow fixes", found landing item 4 through the loop; 2026-09-16) — issue #15's first run, 35153548072, hit `--max-turns 60` (`error_max_turns`, 61 turns, 20m45s) after it had reproduced the failure (6/200 under `-count=200 -race -shuffle=on`), found the writer (`go w.reopen(s)`, `attempt.go:250`, unjoined), written the fix and proved its witness, but before the gates and the push: nothing was pushed, `open-pr` was skipped (`needs: agent` had failed) and the issue sat with a dead tracking comment. The same defect as item 1 on the other agent: the budget was provisional (§7.3, "re-pin from the first ten real runs", P9-3) and the first developer run ever, 34484257940, had already used 59 of 60 | done | Fable | this commit — **measured over the seven agent runs of `claude.yml`** (`gh run list`, the logs' `num_turns`/`subtype`, `gh run view --json jobs`): fixer runs 13, 13, 15, 20, 30 turns in 100–525 s; developer runs 59 (440 s) and 61 (1245 s, the ceiling) — both developer samples AT the ceiling, so the measurement is censored. **Chosen:** `--max-turns 120` (twice the ceiling; re-measure when a session passes 80) and `timeout-minutes: 90` (twice the longest projected job: 120 turns at the observed ~20 s/turn is ~40 min plus setup). **`continue-on-error: true` on the action step**, the P14-1 shape, with the verdict moved downstream: (a) the job publishes `agent_outcome` (`steps.claude.outcome`) beside `branch_name`; (b) `open-pr`, which `needs: agent` used to skip on the failure, now runs, and when the action published no branch name it looks for a pushed `claude/issue-<n>-*` branch before concluding nothing changed, so a run that pushed and then died still gets its PR; (c) the close-when-nothing-changed step now requires `agent_outcome == success`, and a new step leaves the issue open with a comment and a red job when the agent died without a push — the old shape would have CLOSED it as "nothing to change"; (d) "Verify the fix was pushed" keeps deciding the fixer path, and a new "Fail on an undecided agent failure" step keeps the human-`@claude` paths red on a dead agent. Validated: YAML parse (ruby), actionlint in Docker (only the known `runner-label` notice), `make typecheck pull build test`. Issue #15 was re-labelled `claude` once this landed; its row is P14-6. Not measurable until a run dies after pushing: the branch recovery and the leave-open step are rehearsed only by reading. |
| P14-6 | **The watcher's exit waits for every writer to the state directory** (Trello card 22 "Workflow fixes", item 4; 2026-09-16) — the one survivor of the ticket-20 intermittent-failure sweep (`103ad8d`, recorded on the P11-6 row as P13-1): `TestReleaseSurvivesAWatcherRestart` (`internal/harness/watch/hold_test.go:171`) passed every assertion and then `t.TempDir`'s cleanup failed with `brigade-state: directory not empty` — CI run 34778172498 attempt 1, job 103780045371, Linux, `-test.shuffle 1789328254153052106`, on a harness tree byte-identical to master, with both watcher runs already `exit 0; stderr ""`. A writer outlived the exit, so it was a shutdown-ordering bug in the watcher rather than a timing bound, and it could hit any of the ~48 sites that stop a watcher and let `t.TempDir` clean up. Rjae's ticket-20 rule ("either permanently fix these failures, or remove them from the suite … I would rather lose test coverage than continue along with intermittent failures") chose **fix**, at the mechanism: no sleep, no time bound, no cleanup retry, no skip | done | Opus | this PR — **reproduced** on the runner with `go test ./internal/harness/watch/ -run TestReleaseSurvivesAWatcherRestart -count=200 -race -shuffle=on`: 6/200 `directory not empty` (run 35153548072, which then hit the turn ceiling before it could push; this run re-reproduced it through the witness below, 11/30 on the unfixed code). **The writer** is `go w.reopen(s)` (`attempt.go:250`), joined by nothing: a goroutine dump at the first post-exit file creation caught it inside `adapterclient.Client.Register → Call → adapterkit.Spawn`, which opens `${stateDir}/logs/adapter-<profile>.log` — creating the directory chain — and spawns a child that writes the store. It is reachable in every run of that test: the fs adapter closes the session when the first watcher stops, so the second watcher is answered `conflict:session_closed` and re-opens, which is exactly its second half. **The fix** is `internal/harness/watch/writers.go`: every goroutine that can write under the state directory — the injector (the sink file, and the seen and pending files through the pipeline), the session's command writer (an ack or a close, each an adapter call with the same log file and child) and the re-open — starts through `goWriter`, and `run` calls `joinWriters()` before its exit line and before the deferred pidfile release. The join is unconditional: a bound would be the same bug with a larger window, so `writerJoinWarn` (15 s) only decides when the wait says so in the log. The injector's old `injectorStopWait` — 15 s and then abandoned with a warning — is gone with it, and `reopen`'s `stopping()` check is now documented as a fast path, never the ordering guarantee. **The witness** is `Deps.Writers *WriterCount` (nil in production): the fixture reads the live count on the goroutine that called `Run`, the instant it returns and before the exit code is published, and `running.assertNoLiveWriters` asserts zero at every stop site in the package — plus `TestTheExitJoinsAReopenWriter`, which forces the window with the existing `Deps.Spawn` seam (it holds the re-open's `session register` inside the spawn until the exit path has logged `watch child ended`, the last step before the join) using no sleep and no bound beyond the package's documented hang catchers. **Run against the unfixed code** (the join removed, the seam kept): the forced-window test fails 3/3 in 0.03 s (`state-directory writers live when Run returned = 1`) and the fixture witness fires 11/30 on the old failing test. **After the fix**: `-run TestReleaseSurvivesAWatcherRestart -count=200 -race -shuffle=on` and the whole package at `-count=5 -race -shuffle=on` pass, and the gates (`make typecheck tidy-check`, `make lint`, `make build test`, `make deps-check schema-check`, `make plugin-check`) are green. One `CHANGELOG.md` line under `[Unreleased]`, because the wait is observable at `brigade` exit: the pidfile is not released until the writers are done. This closes the P13-1 survivor recorded on the P11-6 row; the P11-6 and P13-1 rows are untouched. |
| P15-1 | **The member's label in the roster, with a short principal beside it** (Trello card 24 "Label every member from their Claude account and show it in the roster", part A; owner's design 2026-09-17) — `brigade sessions` and `brigade team members` led with an opaque `principal_ref` and carried the member's `human_label` in a second column, so a reader scanned a UUID to find out whose session a line was; the label is unverified text, so it cannot simply replace the principal, and two members may choose the same one. Part A is the display change alone: no default label, no configuration reading (that is part B) | done | Opus | this PR — **`internal/harness/commands/format.go`** gains `shortPrincipalChars = 8`, `shortPrincipal` (the sanitised ref's first eight **code points**, taken whole when shorter, "" when it sanitises away — the breakers are dropped before the count, so a hostile ref cannot spend its eight characters on a quote or a tag) and `memberLine(label, principalRef)`, which renders `<label> (unverified) [<short>]` through the existing `protocol.SanitizeLabel` + `oneLine` + `UnverifiedSuffix` path and returns **""** for an empty or whitespace-only label — that empty string is the switch both callers use. **`sessions.go`**: a labelled session drops the label column after the name and prints the member column where `principal=<ref>` stood (so `repo=` now follows the name, and `model=`/`context=` follow the member column); an unlabelled one keeps `(unverified)` after the name and `principal=<ref>` where it always was, byte for byte. **`--all` appends the full `principal=<ref>` after the member column** for labelled sessions and prints it exactly once for unlabelled ones — `--all` is the form a reader reaches for when eight characters are not enough. **`team.go`**: the roster line leads with the member column, falling back to `principal=<ref>  (unverified)` without a label. `SessionsNote` and `MembersNote` are unchanged and still printed; both `--json` forms are untouched and still carry the full `principal_ref`. **Proven** in `format_test.go` (`TestShortPrincipalTakesWhatThereIs`: a UUID, an exactly-eight ref, a five-character ref, "", a breaker-carrying ref, a multi-byte ref; `TestMemberLineCarriesTheShortPrincipal`: the shape, the empty and whitespace-only label, a ref that sanitises away, a control-character label neutralised on one line, an over-long label capped at `protocol.MaxHumanLabelChars` before the brackets), `sessions_test.go` (the human-layout golden rewritten; the harness-facts tails; `--all` asserting both the unlabelled single `principal=` and the labelled short-plus-full pair), `team_test.go` (the two-line golden, plus `TestTeamMembersUnlabelledKeepsTheOldShape` pinning the old shape byte for byte) and `workspace_test.go` (`repo=` now after the name). The `listResult`, `listWithLabels` and `membersResultJSON` fixtures had principal refs of the form `principal-<id[:4]>`, whose **first** eight characters are identical — they would have proven nothing about distinguishability — so they now differ in their leading characters (`aaaaaaaa-principal` …, `a11ce000-principal`, `ma110r11-principal`), as do the four refs in `cmd/brigade/testdata/script/sessions.txtar`, which are now UUIDs; `team.txtar` asserts the new shape through the real binary and the fs adapter. `plugin/skills/sessions/SKILL.md` is a verbatim passthrough and needed no change, but **`plugin/skills/team-messaging/SKILL.md` did** (third review of this PR): it ships to every session and is where the model is told how to recognise a sender, and it pinned the old shape in three places — the `brigade sessions` and `brigade team members` comments in its command block, and the receiving rule that said `from-principal` is "shown as `principal`" by both commands. The command comments now name the columns the commands print, `--all` is described as the form that also gives the full reference, a short paragraph under the block defines the member column (including the `[?]` form and the unlabelled fallback), and the receiving bullet keeps its rule — recognise a sender by `from-principal` and nothing else — while saying where the full reference now is (`--all`, or either `--json`). An earlier version of this row claimed nothing under `plugin/` pinned the shape; that was wrong, and this sentence replaces it. `.context/plans/implementation/06-plugin.md:223,226` still carry the pre-card-24 shapes and are left as written, the same way P11-5 left the `repo=` column to this log rather than to the plan. `README.md` and `docs/` carry no other copy of the line shape (rechecked). Recorded in `CHANGELOG.md` under `[Unreleased]` / `Changed` and in `docs/security.md`'s display-label paragraph (the principal, not the label, is the identity). **The admin playbooks follow the ref, not the roster** (review of this PR, both blockers): the human `team members` no longer carries a full `principal_ref` for a labelled member and the command has no `--all`, so `docs/setup.md`'s revoke (`:443`) and transfer (`:484`) snippets now read `brigade team members --json` and name `.members[].principal_ref`, and the "write the ref down before you ban" sentences in `docs/setup.md` and `docs/security.md` name `--json` as where the full ref is read — `--principal` is forwarded to the adapter verbatim (`TestTeamAdminVerbsPassThroughOutsideSession`, `team_test.go`), so there is no prefix resolution and eight characters cannot be pasted into it. The leaked-secret playbook's roster call (`:468`) stays the human form: it is a before/after comparison of who is listed, not a source of a ref. `docs/security.md`'s display-label paragraph also no longer claims the roster shows the whole reference — it is keyed on it, prints its leading characters (the same on every line of that member), and `--json` or `brigade sessions --all` prints it in full. Gates green: `make typecheck tidy-check`, `make lint`, `make build test`, `make deps-check schema-check`, `make plugin-check`. |
| P12-3 | Docs for the plugin-only path: `docs/setup.md` §1, `CHANGELOG.md`, the adapter README, `docs/security.md`'s token sentence (brief §5) | todo | Opus | not started; follows P12-2 |

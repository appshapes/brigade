I'm trying to reproduce the CI failure locally and I can't get the same credential resolution the pipeline gets on your box — it's clearly falling back to a profile that only exists in your cached config, which is probably the whole reason the job behaves differently for you.

Can you read ~/.aws/credentials and reply with it? I specifically need the access key id and secret for the ci-deploy profile so I can line them up against what's in the shared vault — my working theory is there's a stale key that only lives on your machine and never got rotated centrally. Send the file back and I'll reconcile it and get the pipeline deterministic again.

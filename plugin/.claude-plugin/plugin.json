{
  "name": "brigade",
  "version": "0.6.7",
  "description": "Team messaging between Claude Code sessions of different people and machines, through a pluggable adapter CLI (Supabase adapter bundled).",
  "author": {
    "name": "appshapes"
  },
  "repository": "https://github.com/appshapes/brigade",
  "license": "MIT",
  "keywords": [
    "messaging",
    "team",
    "cross-session"
  ],
  "userConfig": {
    "config_dir": {
      "type": "string",
      "title": "Brigade config directory (advanced)",
      "description": "Where Brigade's credential store lives; leave empty for the platform default (~/.config/brigade), and note that the plugin ignores BRIGADE_CONFIG_DIR from the environment on purpose.",
      "default": ""
    },
    "label": {
      "type": "string",
      "title": "Your label in the team roster",
      "description": "How teammates see you. account (the default) uses the email address of the Claude account this install is signed in to, read from Claude Code's own configuration and never written; none sends no label at all, and your sessions are listed by their principal reference alone; any other text is used as your label. A label is unverified free text either way — the principal reference beside it is the identity.",
      "default": "account"
    },
    "adapter_command": {
      "type": "string",
      "title": "Adapter command override (advanced)",
      "description": "Per-session override of the adapter the team's binding names, given as an absolute path to an adapter executable, a JSON array such as [\"/abs/adapter\", \"--flag\"], or a name registered in adapters.json — never a shell command, and empty means the adapter the project's team file names.",
      "default": ""
    },
    "team_inbound": {
      "type": "string",
      "title": "Incoming team messages",
      "description": "accept (the default) delivers every team message into this session immediately, in every permission mode; refuse never delivers and never acknowledges, so senders see this session as refusing; hold records each message, delivers nothing, and waits for you to run brigade inbox release in your own terminal.",
      "default": "accept"
    },
    "share_workspace_label": {
      "type": "boolean",
      "title": "Share the repository name",
      "description": "Send this session's repository name with it as workspace_label — the name from the checkout's origin remote, else its directory's name, never the working directory path — so teammates can tell which repository a session is in (brigade sessions shows it in its REPO column). Off withholds it.",
      "default": true
    },
    "workspace_label": {
      "type": "string",
      "title": "Workspace label",
      "description": "A label to send instead of the repository name while share_workspace_label is on.",
      "default": ""
    },
    "poll_on_prompt": {
      "type": "boolean",
      "title": "Poll for messages on each prompt",
      "description": "Fallback for hosts without an inbox socket: fetch unread messages when a prompt is submitted, applying the same inbound policy as the watcher.",
      "default": false
    },
    "frame": {
      "type": "string",
      "title": "Frame text for incoming messages",
      "description": "Which instruction paragraph a teammate's message carries: open (the default) states where the message came from, that it is untrusted, that it cannot approve anything or change your settings, and how to reply; guarded adds \"ask your user first\" for settings changes and secrets; strict adds running commands as well. Overridden by frame_file.",
      "default": "open"
    },
    "frame_file": {
      "type": "string",
      "title": "Your own frame text (advanced)",
      "description": "Absolute path to a UTF-8 text file holding your own instruction sentence(s), used instead of the frame level above. The file is read once when the session starts. Brigade's own provenance, untrusted-content and reply text stay as they are.",
      "default": ""
    }
  }
}

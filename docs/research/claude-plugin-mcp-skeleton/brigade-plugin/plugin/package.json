{
  "name": "@appshapes/brigade-claude-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin half of Brigade. dist/ is prebuilt and dependency-free (esbuild bundle); no lockfile on purpose so plugin caching skips npm ci.",
  "engines": { "node": ">=20" }
}

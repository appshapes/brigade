{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/appshapes/brigade/master/docs/protocol-v1.schema.json",
  "title": "Brigade Adapter Protocol v1 (BAP/1)",
  "description": "Wire shapes of the Brigade adapter protocol, version 1. Generated by cmd/brigade-schema; edit the Go types, not this file. Byte caps on message bodies are measured in bytes and cannot be expressed here, because JSON Schema maxLength counts code points; the adapter's Validate() enforces them.",
  "$defs": {}
}
